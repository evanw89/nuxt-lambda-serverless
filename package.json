{
  "name": "nuxt-lambda-serverless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "deploy": "npm run build && sls deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-serverless-express": "^3.3.5",
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "nuxt": "^2.4.2"
  },
  "devDependencies": {
    "serverless-apigw-binary": "^0.4.4"
  }
}
